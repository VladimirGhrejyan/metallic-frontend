export interface IData {
    title: string;
    code: string;
    costPrice: number;
    markup: number;
    categoryId: number;
}

export interface ITableHeader {
    id: string;
    title: string;
}
